// Declaramos una constante y le asignamos la función sin nombre
const presentarPersona = function(nombre, profesion) {
  // Unimos las piezas de texto
  console.log(nombre + ' es ' + profesion);
};

// Pruebas
presentarPersona('Juan', 'desarrollador');
presentarPersona('Andrés', 'estudiante de IA');